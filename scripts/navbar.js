/* ---------------------------------------------------
   NAVBAR.JS — Only needed if you want a shared toggle
--------------------------------------------------- */

function toggleMenu() {
    const menu = document.getElementById("mobileMenu");
    if (menu) {
        menu.classList.toggle("show");
    }
}
